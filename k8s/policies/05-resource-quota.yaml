# ============================================================
#  ResourceQuota — portfolio namespace
#
#  Cluster capacity: 2 × 6 vCPU / 16Gi nodes = 12,000m / 32Gi total
#  85% ceiling:      10,200m CPU  |  27,852Mi memory
#
#  Portfolio peak (rolling update + job + backup CronJobs simultaneous):
#    requests.cpu:    ~1,430m
#    requests.memory: ~2,496Mi
#    limits.cpu:      ~5,900m
#    limits.memory:   ~5,792Mi
#    pods:            ~12
#
#  Quota = peak × 1.5 growth factor, verified against cluster 85% ceiling.
#  Cluster utilisation at quota maximum: ~42% CPU, ~27% memory — safe.
# ============================================================

apiVersion: v1
kind: ResourceQuota
metadata:
  name: portfolio-quota
  namespace: portfolio
  labels:
    app.kubernetes.io/part-of: portfolio-mcs
    app.kubernetes.io/managed-by: argocd
spec:
  hard:
    requests.cpu:    "3"
    requests.memory: "4Gi"
    limits.cpu:      "8"
    limits.memory:   "10Gi"
    pods:            "20"
    services:        "10"
    persistentvolumeclaims: "3"
